# ===============================================================================
# MarketPulse Analytics - Environment Configuration
# ===============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control

# ===============================================================================
# Google Cloud MySQL Configuration
# ===============================================================================
# Your MySQL database is hosted on Google Cloud Platform
DB_HOST=34.133.0.30
DB_PORT=3306
DB_NAME=databaseproj
DB_USER=root
DB_PASS=YourPasswordHere
# Note: URL-encode special characters in password (e.g., @ becomes %40)

# ===============================================================================
# Firestore Configuration (Google Cloud)
# ===============================================================================
# Firestore uses Application Default Credentials (ADC)
# The client automatically looks for credentials in this order:
# 1. GOOGLE_APPLICATION_CREDENTIALS (if set - points to service account key file)
# 2. gcloud CLI credentials (if authenticated via: gcloud auth application-default login)
# 3. GCE/Cloud Run service account (if running on Google Cloud)

# OPTION 1: Use gcloud CLI (RECOMMENDED for local development)
# Run this command once:
#   gcloud auth application-default login
# No need to set GOOGLE_APPLICATION_CREDENTIALS

# OPTION 2: Use service account key file (Alternative)
# Uncomment and set path to your service account key:
# GOOGLE_APPLICATION_CREDENTIALS=api_python/service_key.json
# Or use absolute path: /path/to/service_key.json

# Firestore database ID (use 'databaseproj' or leave empty for default)
FIRESTORE_DATABASE=databaseproj

# ===============================================================================
# NewsAPI Configuration
# ===============================================================================
# Get your API key from: https://newsapi.org/register
NEWSAPI_KEY=your_newsapi_key_here
# Free tier: 100 requests per day
# Pay attention to rate limits

# ===============================================================================
# Application Settings
# ===============================================================================
# FastAPI server configuration
API_HOST=0.0.0.0
API_PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Cache and request limits
NEWS_CACHE_TTL=300
MAX_ARTICLES_PER_REQUEST=50

# ===============================================================================
# Optional: Local Development (if using Docker)
# ===============================================================================
# Uncomment and configure if running locally with Docker
# LOCAL_DB_HOST=localhost
# LOCAL_DB_PORT=3307
# LOCAL_MONGO_HOST=localhost
# LOCAL_MONGO_PORT=27017

# ===============================================================================
# Notes for Team Members
# ===============================================================================
# 1. Create a .env file by copying this template:
#    cp env.example .env
#
# 2. Fill in your actual credentials
#
# 3. Never commit .env to Git (it's in .gitignore)
#
# 4. For Google Cloud access, contact your team lead for credentials
#
# 5. For NewsAPI key, sign up at https://newsapi.org/register
#
# ===============================================================================
